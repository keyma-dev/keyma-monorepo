// The UI frontend domain (registered by the CLI via `config.domains`, alongside the schema
// domain). The UI smoke defines no diagnostic codes of its own (a future real UI domain would
// take its own disjoint KEYMA#### band per CLAUDE.md).
export { uiFrontendDomain } from "./ui-frontend.js";
