// The schema frontend domain (registered by the CLI via `config.domains`) plus the
// schema-domain diagnostic codes.
export { schemaFrontendDomain } from "./schema-frontend.js";
export { synthesizeClassMembers, type SynthesizeDeps } from "./synthesize.js";
export * from "./diagnostics.js";
